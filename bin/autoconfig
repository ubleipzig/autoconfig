#!/usr/bin/env node

'use strict';

var program = require('commander');

program
	.version('1.4.2')
	.description('creates the config for a site and instance accordingly to defaults and custom overrides')
	.command('vufind', 'sets up a vufind instance')
	.parse(process.argv);
